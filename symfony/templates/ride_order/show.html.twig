{% extends 'base.html.twig' %}

{% block title %}RideOrder{% endblock %}

{% block body %}
    <h1>RideOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ride_order.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ ride_order.status }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ ride_order.createdAt ? ride_order.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ride_order_index') }}">back to list</a>

    <a href="{{ path('app_ride_order_edit', {'id': ride_order.id}) }}">edit</a>

    {{ include('ride_order/_delete_form.html.twig') }}
{% endblock %}
